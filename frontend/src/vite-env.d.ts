/// <reference types="vite/client" />
/// <reference types="vite-plugin-pwa/react" />

/** Versão publicada, injetada no build (ver `define` no vite.config.ts). */
declare const __VERSAO_APP__: string;
