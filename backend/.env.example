DATABASE_URL="postgresql://postgres:postgres@localhost:5432/diario_de_obra?schema=public"
JWT_SECRET="troque-este-segredo-em-producao"
PORT=3333
UPLOADS_DIR="uploads"

# Integração com a planilha do cliente via Microsoft Graph API
# Ver App registration no Azure/Entra ID do tenant do cliente (permissão de aplicativo Files.ReadWrite.All)
MS_GRAPH_TENANT_ID=""
MS_GRAPH_CLIENT_ID=""
MS_GRAPH_CLIENT_SECRET=""
MS_GRAPH_USER_UPN=""
# Link de compartilhamento do arquivo no SharePoint/OneDrive for Business (formato "https://.../:x:/s/...")
MS_GRAPH_SHARE_URL=""

# Onde as fotos dos registros são arquivadas (opcionais: já têm valor padrão no código).
# Caminho do site no formato exigido pelo Graph e nome da biblioteca de documentos.
MS_GRAPH_SITE_PATH=""
MS_GRAPH_BIBLIOTECA_FOTOS=""

# Webhook do fluxo n8n que grava os registros na planilha real do cliente
# (workflow "TESTE - Microsoft Graph SharePoint TES", nó "Webhook")
N8N_WEBHOOK_URL=""
